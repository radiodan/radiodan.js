port                    "{{{port}}}"
music_directory         "{{{music}}}"
playlist_directory      "{{{playlist}}}"
db_file                 "{{{db}}}"
log_file                "{{{log}}}"
audio_output {
    type "osx"
    name "CoreAudio"
    mixer_type "software"
}

